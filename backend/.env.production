# Render Deployment Configuration

# This file helps Render deploy your backend automatically

# Build command - installs dependencies
# Render will run this command
# (Already configured in dashboard)

# Environment variables for Render:
# Add these in Render Dashboard → Environment
# NODE_ENV=production
# PORT=10000
# DATABASE_URL=postgresql://user:pass@host/dbname

# Example .env.production (do NOT commit with real values!)
# NODE_ENV=production
# DATABASE_URL=postgresql://user:password@host:5432/bulk_mailer_db
# JWT_SECRET=your-secret-key
# SMTP_PASSWORD=your-email-password

# Services to create:
# 1. Web Service (Backend)
#    - Build: cd backend && npm install
#    - Start: cd backend && npm start
#
# 2. PostgreSQL Database (256MB free)
#    - Create new database
#    - Copy connection string to DATABASE_URL

# For keeping service awake (free tier):
# Use UptimeRobot.com and ping the /health endpoint every 10 minutes
# Service URL: https://bulk-mailer-backend.onrender.com
# Health check endpoint: GET /health
